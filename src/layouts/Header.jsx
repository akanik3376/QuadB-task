import React from 'react';
import Navbar from '../components/HeaderComponent/Navbar';

const Header = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Header;
